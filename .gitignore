# Worktrees
.worktrees/

# Private research (never push)
config.local/
edge_catcher/hypotheses/local/
edge_catcher/monitors/strategies_local.py
docs/superpowers/
docs/research-pipeline-data-flow.md

# Replay-parity fixtures contain per-trade timing/size/price data and
# strategy/series identifiers — regenerated locally via
# tests/fixtures/replay_parity/regenerate.py against bundles in data/bundles/.
# Sentinel + allowlist files are tracked (small, no per-trade data).
tests/fixtures/replay_parity/*.expected.json
tests/fixtures/replay_parity/.fixtures_present

# Data and databases
data/
*.db
*.db-wal
*.db-shm

# Tracked example fixtures — small, deterministic DBs bundled for the
# quickstart + reporting tests. Re-build with tests/fixtures/build_demo_*.py.
!edge_catcher/data/
!edge_catcher/data/examples/
!edge_catcher/data/examples/*.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
venv/
*.egg
.eggs/

# Reports output (entire directory is private)
reports/
/TASK.md
/BACKLOG.md
/ROADMAP.md

# Environment
.env
*.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# pytest
.pytest_cache/
.coverage
htmlcov/

# Logs
*.log
logs/

# Credentials (never commit API keys)
*.pem
*.key
.secrets/
credentials.json
node_modules/

# Temporary sweep/test scripts (all run_* are private research)
run_*.py
!edge_catcher/research/run_queue.py
TASK.md
TASK-*.md

# Local strategies and custom hypotheses (edge-specific, never commit)
edge_catcher/runner/strategies_local.py
edge_catcher/hypotheses/custom/

# Private research scripts and analysis (entire folder)
scripts/

# Strategy-specific tests (reveals strategy logic)
tests/test_flow_*.py
tests/test_local_*.py
tests/test_strategies_paper.py

.superpowers/
.claude/

# Backup files
*.bak

# Pi strategy scratch files (from scp/handoff)
pi-*.py

# Export bundles (Pi handoff zips)
exports/

paper-trader.yaml

# Local private-infra paper-trader log stash (never tracked)
private-infra_paper_trader_logs/